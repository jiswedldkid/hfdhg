print("简单计算器")
print("支持：+ 加法, - 减法, * 乘法, / 除法")

while True:
    # 获取用户输入，例如：5 + 3
    user_input = input("\n请输入算式（例如：5 + 3），输入 q 退出：").strip()
    
    if user_input == 'q':
        print("再见！")
        break
    
    try:
        # 直接用 eval 计算（简单但在实际项目中需注意安全）
        result = eval(user_input)
        print(f"结果：{result}")
    except:
        print("输入有误！请检查格式，例如：10 * 2")
